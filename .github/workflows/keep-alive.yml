name: Keep Render Service Alive

on:
  schedule:
    - cron: '*/7 * * * *' # 매 7분마다 실행
  workflow_dispatch: # 수동 실행 가능

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Service
        run: curl -I https://assetsmngtest.onrender.com
